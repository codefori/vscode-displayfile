on:
  pull_request:
    paths:
      - 'src/**'
    
jobs:
  release:
    name: Test runner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
          submodules: true
      - run: npm ci
      - run: npm run test